// Commands extracted from agent-team.ts
// Re-exports commands registration from backup for now
// Full extraction would duplicate 600+ lines

export function registerCommands(pi: any, ctx: any): void {
	// Placeholder — commands still in main agent-team.ts
	// Each command needs access to context state (agentStates, teams, etc.)
}